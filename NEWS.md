# schtools development version

- Added a `NEWS.md` file to track changes to the package.
- Major new functions:
  - `read_dist()`
  - `read_tax()`
  - `inline_hook()`
  - `paste_oxford_list()`
  - `format_number()`
- New vignettes:
  - `introduction`
